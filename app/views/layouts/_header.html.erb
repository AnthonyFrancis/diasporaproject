<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "TheDiasporaProject", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to "Syndicates", syndicates_path %></li>
        <!--<li><%#= link_to (raw ("Companies <small>(coming soon)</small>")), companies_path %></small></li>-->
        <li><%= link_to "People", people_path %></li>
        <% if user_signed_in? %>

        <% else %>

        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">


        <% if user_signed_in? %>

          <% if current_user_subscribed? %>


          <% else %>

            <% if trial_expired? %>
              <li><%= link_to "<p>Go Premium</p>".html_safe, subscription_path, class: "text-center premium" %></li>
            <% else %>
              <li>Trial Ends in <%= remaining_days %>
                days<%= link_to "<p>Go Premium</p>".html_safe, subscription_path, class: "text-center premium" %></li>
            <% end %>

          <% end %>

          <% if current_user.investor == true %>
            <li><%= link_to 'Create Syndicate', new_syndicate_path %></li>
          <% end %>

          <li>
            <%= link_to mailbox_inbox_path do %>
              Inbox <span class="notifications-count label label-danger pull-right"><%= current_user.unread_inbox_count %></span>
          <% end %>
          </li>

          <li class="dropdown">

            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= image_tag(current_user.photo.url(:thumb), :class => "round-image-28") %>
              <span class=""></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to current_user.name, user_path(current_user) %></li>
              <li><%= link_to "Settings", settings_path %></li>
              <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Join", new_user_registration_path %></li>
          <li><%= link_to "Log in", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>