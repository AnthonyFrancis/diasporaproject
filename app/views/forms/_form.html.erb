<%= form_for(@form) do |f| %>
  <% if @form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form.errors.count, "error") %> prohibited this form from being saved:</h2>

      <ul>
      <% @form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :investment_pledge %><br>
    <%= f.text_field :investment_pledge %>
  </div>

  <div class="field">
    <%= f.label :confirm_backer %><br>
    <%= f.check_box :confirm_backer, autofocus: true %>
  </div>

  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
